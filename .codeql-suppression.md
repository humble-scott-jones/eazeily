# CodeQL Suppression File
# 
# This file documents false positives from CodeQL analysis

# False Positive: Logging Secret IDs (not values)
# Rule: py/clear-text-logging-sensitive-data
# File: secrets_util.py
# 
# CodeQL incorrectly flags our logging statements as "logging sensitive data"
# However, we are only logging:
#   - secret_id: The NAME/ID of the secret (e.g., "stripe-secret-key"), NOT the value
#   - fallback_env: The NAME of an environment variable (e.g., "STRIPE_SECRET_KEY"), NOT its value
#   - error_type: The exception type
# 
# We explicitly never log the actual secret values. All logging statements include
# comments clarifying this, such as:
#   # Safe: Only logging the secret ID (name), not the secret value itself
#   # Safe: Only logging the env var name, not its value
# 
# These are false positives and can be safely suppressed.
# 
# Affected lines in secrets_util.py:
# - Line 66: logger.info(f"Accessing secret: {secret_id}", ...) - Logs ID only
# - Line 94: logger.info(f"Successfully fetched secret from Secret Manager: {secret_id}") - Logs ID only
# - Line 106-107: logger.error(..., extra={"secret_id": secret_id}) - Logs ID only
# - Line 116: logger.info(f"Using environment variable fallback: {fallback_env}") - Logs var name only
# - Line 125: logger.error(error_msg) - Error message doesn't contain secret values
# - Line 156: logger.debug(f"Secret not found: {secret_id} ...") - Logs ID only
# - Line 193: logger.warning(f"Failed to preload secret {secret_id}: ...") - Logs ID only
# - Line 196: logger.info(f"Preloaded {count}/{total} secrets") - Just counts, no secret values
#
# Verification:
# You can verify these are false positives by reviewing secrets_util.py:
# 1. All functions that return secret values explicitly return them, they never log them
# 2. All log statements include clarifying comments
# 3. The only things logged are: secret IDs, env var names, counts, and error types
# 4. Tests verify that secret values are never logged (test_secret_not_logged)

# Security Summary:
# - NO actual secret values are ever logged
# - Only secret identifiers (names) are logged for audit purposes
# - This is standard security practice for audit logging
# - Tests confirm secret values are not present in logs
