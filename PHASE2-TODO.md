# Phase 2 — Reels polish (starter tasks)

This branch scaffolds Phase 2 work focused on the reels generator and supporting tests.

Tasks:

- [ ] UI: Ensure inline "Generate Reels" button is visible on `/app` and `index.html` for paid users.
- [ ] Backend: Add small endpoint or improve webhook handling to mark users paid quickly for E2E tests.
- [ ] Tests: Add unit tests for reel quota enforcement and free-sample gating (happy path + quota exceed).
- [ ] CI: Add a workflow job to run unit tests on PRs and run full E2E on `main`.

Notes:
- Keep changes small and test-driven. Create follow-up PRs per task.
